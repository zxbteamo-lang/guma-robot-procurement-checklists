# 多机器人管理平台 RFP：混合机器人接入前必须验收的 15 项测试

机器人管理平台经常被演示成一个后台：添加机器人、显示电量、放到地图上，再提供一个“执行任务”按钮。

这足以说明界面流程，不足以证明平台可以长期运营。采购方真正要验收的是机器人、适配器、地图、任务、权限、业务系统和人工接管之间的控制契约。

## 先分清三层责任

1. **机器人与原厂系统层：** 负责底层运动、导航、保护行为、设备健康和型号特有限制。
2. **多机器人编排层：** 负责任务分配、共享资源协调、跨机器人状态和运营策略。
3. **业务运营层：** 负责订单、工单、巡检计划、账号权限、证据、报表和服务流程。

Web 平台不应绕过机器人已经验证的安全控制；原厂机器人管理器也不会自动实现客户的业务流程。

## 15 项 RFP 验收测试

### 1. 机器人与软件身份

记录型号、硬件版本、控制器、固件、导航栈、原厂管理软件、SDK、适配器和平台版本。

验收证据：机器可读设备清单，以及人可读的支持版本矩阵。

### 2. 能力契约

为每种机器人列出平台可以观察、请求、远程辅助和直接控制的能力，并区分支持、不支持、模拟和现场受限。

验收证据：能力矩阵、准确接口名称和限制条件。

### 3. 适配器行为

定义原厂 API、ROS 接口、消息和错误码怎样映射到稳定的内部契约，包括时间戳、单位、坐标系、限速、超时、幂等和重试规则。

验收证据：接口 schema、测试夹具和错误映射样例。

### 4. 地图与坐标所有权

说明地图、楼层、区域、点位、充电点、门、电梯和禁行区由哪个系统管理，并记录不同机器人地图之间的关系。

验收证据：版本化场地模型、坐标变换、审批流程和回滚测试。

### 5. 完整任务生命周期

测试接收、分配、执行、暂停、阻塞、失败、取消、恢复、人工完成和过期，而不是只有“排队”和“完成”。

验收证据：正常路径与异常路径的状态转换日志。

### 6. 跨机器人任务分配

验证哪些机器人可以接收任务，以及能力、位置、电量、负载、权限、维护状态和优先级如何参与决策。

验收证据：可重复的分配案例和完整决策输入。

### 7. 共享资源协调

测试窄通道、交叉口、门、电梯、工位、充电区和人工作业区。地图上的动画不等于物理资源已经协调。

验收证据：冲突案例、等待规则、超时和恢复日志。

### 8. 电量与充电策略

确认预留电量、充电器兼容性、排队、任务中断、充电失败和首选充电位占用后的处理。

验收证据：一次完整低电量运行与恢复。

### 9. 网络退化

注入延迟、丢包、断网和重连，明确哪些能力留在机器人本地，以及通信恢复后平台允许请求什么。

验收证据：安全状态、有界重试、无重复执行，以及可重建时间线。

### 10. 适配器与服务重启

分别重启机器人适配器、任务服务、数据库、消息中间件和前端，检查任务所有权是否丢失或重复执行。

验收证据：重启矩阵、持久状态、对账结果和操作员告警。

### 11. 人工接管

定义谁可以暂停、遥控、释放、恢复或取消任务，人工操作不能破坏分析失败所需的证据。

验收证据：角色校验、命令审计、任务交还规则和接管后状态。

### 12. 权限与审计

区分操作员、工程师、管理员、服务商和审计员。地图发布、远程移动、软件更新、数据导出和安全设置需要明确授权。

验收证据：权限矩阵，以及高风险操作不可缺失的审计事件。

### 13. 日志、回放与工作证据

把机器人、地图、任务、适配器、平台和业务系统版本与状态变化、错误、人工干预和任务证据关联。

验收证据：不依赖操作员记忆，从原始记录回放一次失败任务。

### 14. 企业系统集成

测试 WMS、MES、EAM、工单、巡检、身份或客户应用接口，同时覆盖重复消息、下游不可用、延迟确认和对账。

验收证据：API 契约、重试规则、死信处理和端到端追踪编号。

### 15. 升级与回滚

先升级一个适配器或一组机器人，再逐步扩大；定义兼容窗口，以及不破坏地图、任务和运营记录的回滚方式。

验收证据：灰度升级、不兼容版本阻断和已验证回滚。

## 更安全的 PoC 顺序

1. **单机、单场地、一个代表性任务：** 先跑通完整数据与恢复闭环。
2. **失败注入：** 测试断网、定位丢失、路线受阻、低电量、服务重启和人工接管。
3. **第二种机器人或第二家厂商：** 验证适配器和能力模型没有掩盖差异。
4. **共享资源与业务系统：** 前三阶段通过后，再加入交通策略、门、电梯、权限、报表和企业接口。

扩展规模应是证据的结果，不应是第一次演示的目标。

## 交付时必须拿到什么

- 支持的机器人与版本矩阵；
- 架构与责任边界；
- 适配器和业务 API 契约；
- 版本化地图与任务资产；
- 角色与权限矩阵；
- 部署、升级和回滚步骤；
- 验收脚本与失败案例；
- 日志、回放说明和已知限制；
- 培训、运维与故障恢复手册。

## 公开参考与证据边界

[Open‑RMF](https://github.com/open-rmf/rmf) 是公开的多机器人队列管理参考，涉及任务分配、共享空间和基础设施协调；它不证明某个机器人、适配器、场地或 GUMA 版本已经兼容。

[MassRobotics AMR Interoperability Standard](https://www.massrobotics.org/what-is-the-massrobotics-amr-interoperability-standard/) 可用于理解跨厂商基础状态共享；其公开说明明确区分基础信息共享与任务管理、导航和安全系统。

[GUMA Robot Studio](https://www.lllrobots.com/studio.html?utm_source=github&utm_medium=organic_repository&utm_campaign=multi-robot-platform-rfp-zh-github-20260724&utm_content=platform) 公开展示机器人、地图、点位、任务、角色、日志与多种部署方式。独立模拟体验不连接真实客户机器人或数据，不能证明具体型号、接口、并发规模或现场效果已经通过验收。

提交私密项目需求：

<https://www.lllrobots.com/contact.html?intent=studio&utm_source=github&utm_medium=organic_repository&utm_campaign=multi-robot-platform-rfp-zh-github-20260724&utm_content=contact>

本资料是采购和验收模板，不是客户案例、兼容性证明、认证、性能等级、价格、交期、投资回报或交付保证。以上项目事实必须由准确版本、现场测试和书面范围证明。

不要在公开 GitHub Issue 中填写手机号、邮箱、客户名称、账号凭据、保密场地图或精确的非公开场地信息。
